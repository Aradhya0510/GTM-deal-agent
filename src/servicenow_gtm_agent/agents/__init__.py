"""Sub-agent definitions for the deal intelligence pipeline.

The production agent uses a single LangGraph graph with tool-calling loop
(see deployment/agent.py), NOT separate sub-agents. The sub-agent pattern
in this package is for reference/future modular expansion.
"""
